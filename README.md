# Study Valley

เว็บเตรียมสอบ สอวน. คอมพิวเตอร์ · ใช้บนมือถือเป็นหลัก

## สถานะ

ตัวอย่างหน้าแรก — ยังไม่ใช่เวอร์ชันเต็ม

| | สถานะ |
|---|---|
| ดีไซน์ + design token | ✅ |
| ฟอนต์ Layiji (patch แล้ว) | ✅ |
| ภาพ SVG 15 ชิ้น (ไม่ใช้อีโมจิ) | ✅ |
| หน้าแรก `index.html` | ✅ ยังใช้ข้อมูลจำลอง |
| `/posn/` รายการชุด + เอนจินทำโจทย์ | ⬜ |
| `/english/` เกมศัพท์ | ⬜ |
| `data/posn/` 29 ชุด · 559 ข้อ | ⬜ |

## ตอนนี้เป็นไฟล์เดียว

`index.html` รวม CSS + JS + SVG + ฟอนต์ (base64) ไว้ในตัว
— เปิดออฟไลน์ได้ · deploy Vercel ได้เลย ไม่ต้อง build

ตอนทำเวอร์ชันเต็มค่อยแยกเป็น:

```
assets/app.css · app.js · art.js · fonts/Layiji.woff2
```

## สำคัญ

อ่าน **THEME.md** ก่อนแก้อะไร — มีบันทึกบั๊กฟอนต์ Layiji 2 ตัว
(สระบนโดนเฉือน · สระ `ำ` กลายเป็น `า`) และกฎเหล็กที่เคยพังมาแล้ว

## ค้าง

- เปลี่ยนจากข้อมูลจำลองไปอ่าน `data/posn/index.json`
- ยังไม่ตัดสินใจ: สีวิชา (ส้ม/teal หรือ ชมพู/ฟ้า) · คำเชียร์ในหน้าสรุปผล
